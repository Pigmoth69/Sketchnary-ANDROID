<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.game.sketchnary.sketchnary" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Dark" >
        <activity
            android:name="com.game.sketchnary.sketchnary.Main.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.game.sketchnary.sketchnary.Authentication.LoginActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="com.game.sketchnary.sketchnary.Authentication.SignupActivity" android:theme="@style/AppTheme.Dark" />
    </application>

</manifest>
